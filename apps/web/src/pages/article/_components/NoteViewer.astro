---
import type { Article } from "@utils/types";
import "@styles/lexical-theme.css"

interface Props {
  note: Article
}

const { note } = Astro.props as Props;
---

<article set:html={note.contentHTML}></article>